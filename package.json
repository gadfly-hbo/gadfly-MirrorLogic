{
  "name": "mirrorlogic",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:client": "npm run dev --workspace=@mirrorlogic/client",
    "dev:server": "npm run dev --workspace=@mirrorlogic/server"
  }
}